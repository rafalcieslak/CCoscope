// This Greatest Common Divisor example was modified
// to make it closer to Kaleidoscope language
fun gcd (a : int, b : int, c : int) : int {
    c := 0;
    while ( != 0 ) {
        c := a;
        a := b % a;
        b := c;
    }
    return b;
}
