// This example can be passed to lli to
// be executed. Check exit code, it should
// be 20!

fun gcd (a : int, b : int) : int {
    var c : int;
    c := 0;
    while ( a != 0 ) {
        c := a;
        a := b % a;
        b := c;
    }
    return b;
}

fun main() : int{
    print(gcd(160,200));
    print(gcd(160,250));
    print(gcd(100,295));
}